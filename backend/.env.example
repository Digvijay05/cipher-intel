# ==============================================================================
# Honeypot API Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to version control.
# ==============================================================================

# ------------------------------------------------------------------------------
# REQUIRED: Authentication
# ------------------------------------------------------------------------------

# API key for x-api-key header authentication
# This is the key GUVI evaluator will use to call your endpoint
# Generate a secure random string, e.g.: openssl rand -hex 32
HONEYPOT_API_KEY=your-secure-api-key-here

# ------------------------------------------------------------------------------
# REQUIRED: LLM Configuration
# ------------------------------------------------------------------------------

# Ollama Cloud API key (Primary LLM Provider)
# Get from: https://ollama.com/settings/keys
OLLAMA_API_KEY=secret-key

# Ollama model to use (optional, defaults to gemma3:27b-cloud)
# Options: gemma3:27b-cloud, llama3.3:70b-cloud, mistral:7b-cloud
# OLLAMA_MODEL=gemma3:27b-cloud

# Ollama base URL (optional, defaults to https://ollama.com)
# OLLAMA_BASE_URL=https://ollama.com

# ------------------------------------------------------------------------------
# OPTIONAL: Fallback LLM Providers
# ------------------------------------------------------------------------------

# Groq API key (Optional - for fallback)
# Get from: https://console.groq.com/keys
GROQ_API_KEY=secret-key

# OpenAI API key (Optional - for fallback or legacy use)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=secret-key
# ------------------------------------------------------------------------------
# OPTIONAL: Logging
# ------------------------------------------------------------------------------

# Log level: DEBUG, INFO, WARNING, ERROR
# Use DEBUG for troubleshooting, INFO for production
LOG_LEVEL=INFO

# Log format: json (structured) or simple (human-readable)
# Use json for production, simple for development
LOG_FORMAT=json

# ------------------------------------------------------------------------------
# OPTIONAL: Session Configuration
# ------------------------------------------------------------------------------

# Maximum messages before session terminates and callback is sent
# Default: 20
MAX_SESSION_MESSAGES=20

# ------------------------------------------------------------------------------
# OPTIONAL: ngrok Configuration (if using ngrok for public access)
# ------------------------------------------------------------------------------

# ngrok authtoken (get from: https://dashboard.ngrok.com/get-started/your-authtoken)
# Only needed if running ngrok in Docker or using premium features
# NGROK_AUTHTOKEN=

# ------------------------------------------------------------------------------
# INTERNAL: Docker Compose Configured (do not change)
# ------------------------------------------------------------------------------

# Redis URL is set by docker-compose.yml
# REDIS_URL=redis://redis:6379/0
